{
  "name": "multi-delegation",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ andyesp/multi-delegation",
    "create-local": "graph create --node http://localhost:8020/ andyesp/multi-delegation",
    "remove-local": "graph remove --node http://localhost:8020/ andyesp/multi-delegation",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 andyesp/multi-delegation",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.46.1",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "dotenv": "^16.0.3",
    "hardhat": "^2.13.0"
  }
}
